import UIKit

class Category {
    let title: String
    // let color: UIColor
    let target: String
    
    init(title: String, target: String) {
        self.title = title
        self.target = target
    }
}
